# Estilos MelodIA

Os tokens de design (cores, fontes, animações) estão definidos em `src/index.css` com `@theme` e `@layer base` (Tailwind v4).

Para estilos específicos de componente, use classes Tailwind ou CSS Modules no futuro, mantendo os tokens centrais aqui se necessário.
